#include "sort.h"

/**
 * merge_sort - sorts an array while printing out the process
 * using merge sort
 * @array: the array to sort
 * @size:size of the array
 */
void merge_sort(int *array, size_t size)
{
	(void)array;
	(void)size;
}
